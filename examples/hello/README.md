# hello

A minimal static site (busybox httpd) used to smoke-test litehouse deploys.

Push this directory to its own GitHub repo, then register it with:
`lh create hello --repo you/repo`

From then on, every `git push` to that repo builds and deploys automatically.
